print("=== Parts with TouchInterest ===")
for _, obj in ipairs(workspace:GetDescendants()) do
    if obj:IsA("TouchInterest") and obj.Parent and obj.Parent:IsA("BasePart") then
        print(obj.Parent:GetFullName())
    end
end
